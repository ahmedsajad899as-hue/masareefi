[build]
builder = "NIXPACKS"
buildCommand = "pip install -r backend/requirements.txt && cd backend && python generate_icons.py"

[deploy]
startCommand = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Persistent volume — keeps SQLite DB alive across deploys
[[deploy.volumes]]
mountPath = "/data"
